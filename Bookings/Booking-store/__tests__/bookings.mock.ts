import { Booking } from '../../../Bookings/Booking/Booking.class';
import { bookMock, book2Mock } from './books.mock';

export const bookingMock = new Booking(bookMock);
export const booking2Mock = new Booking(book2Mock);
